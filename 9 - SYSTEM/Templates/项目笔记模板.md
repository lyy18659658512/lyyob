<%*
let name = (await tp.system.prompt("笔记名称", "", false) || "").trim();
if (name && name !== tp.file.title) { await tp.file.rename(name); }
let tags = await tp.system.prompt("主题标签，多个用英文逗号分隔（如：AI, 学习）", "", false) || "";
let status = await tp.system.suggester(["active", "ongoing", "someday"], ["active", "ongoing", "someday"], false, "项目状态");
let deadline = await tp.system.prompt("截止日期 YYYY-MM-DD（回车跳过）", "", false) || "";
-%>
---
title: <% name || tp.file.title %>
type: project
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
tags: [<% tags %>]
status: <% status || "active" %>
progress: ""
startDate: ""   # 可选，格式 YYYY-MM-DD
deadline: "<% deadline %>"    # 可选，格式 YYYY-MM-DD
links: []
---

# <% name || tp.file.title %>

## 🎯 项目概述



## ✅ 任务清单


## ⚠️ 问题与阻塞



## 🔗 相关资源

- 
