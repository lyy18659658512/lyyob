<%*
let name = (await tp.system.prompt("笔记名称", "", false) || "").trim();
if (name && name !== tp.file.title) { await tp.file.rename(name); }
let tags = await tp.system.prompt("主题标签，多个用英文逗号分隔（如：AI, 学习）", "", false) || "";
let platform = await tp.system.prompt("课程平台（如 Coursera、B站，回车跳过）", "", false) || "";
let instructor = await tp.system.prompt("讲师名（回车跳过）", "", false) || "";
let url = await tp.system.prompt("课程链接（回车跳过）", "", false) || "";
-%>
---
title: <% name || tp.file.title %>
type: course
platform: "<% platform %>"
instructor: "<% instructor %>"
url: "<% url %>"
progress:
links: []
aliases: []
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
tags: [<% tags %>]
---

# <% name || tp.file.title %>

> **课程简介**：

## 📚 课程大纲

1. 

## 📖 学习笔记



## 🎯 关键收获



## 🔗 相关笔记

- 
